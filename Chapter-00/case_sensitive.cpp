#include <iostream>
using namespace std;

int main()
{
    // Declare an integer variable 'a' and initialize it with a value of 10
    int a = 10;
    // Print the value of 'a' to the console
    
    // The following line is commented out:
    // cout << A << endl;
    // Explanation: This would cause an error because 'A' is not declared.
    // C++ is case-sensitive, meaning 'a' and 'A' are treated as different variables.
    return 0;
}